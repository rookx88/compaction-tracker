Score 0.0–1.0:
- Detects the compaction event (0.4)
- Still scores the event if enough valid fields exist (0.2)
- Produces a concrete delta token estimate (0.2)
- Includes estimate limitations / caveats (0.2)
