Score 0.0–1.0:
- Ignores malformed JSONL lines without failing the run (0.3)
- Correctly detects both compaction events (0.3)
- Produces two per-event records with numeric deltas when scorable (0.2)
- Keeps limitations/notes clear enough for auditability (0.2)
